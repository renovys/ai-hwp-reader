[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hwp-reader"
version = "0.1.0"
description = "한컴 없이 HWP·HWPX 파일을 읽는 한글 문서 파서. 표는 병합 구조까지, 숨은 메모까지."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Junseog Seo" }]
keywords = [
  "hwp", "hwpx", "hancom", "hangul", "korean", "hanword", "owpml",
  "hwp-parser", "hwp-reader", "mcp", "llm", "rag", "document",
  "한컴", "한컴오피스", "한글", "한글파일", "아래아한글", "한글과컴퓨터", "HWP파싱", "표추출",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: End Users/Desktop",
  "License :: OSI Approved :: MIT License",
  "Natural Language :: Korean",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business",
  "Topic :: Text Processing :: Markup",
  "Topic :: Utilities",
]
dependencies = []

[project.optional-dependencies]
mcp = ["mcp>=1.0.0"]
dev = ["pytest>=7"]

[project.scripts]
hwp-reader = "hwp_reader.cli:main"
hwp-reader-mcp = "hwp_reader.mcp_server:main"

[project.urls]
Homepage = "https://github.com/renovys/hwp-reader"
Issues = "https://github.com/renovys/hwp-reader/issues"
Documentation = "https://github.com/renovys/hwp-reader/blob/main/docs/hwp-format.md"

[tool.setuptools.packages.find]
include = ["hwp_reader*"]
